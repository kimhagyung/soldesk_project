package kr.co.softsoldesk.beans;

public class ProUserBean {
	/*
	 * CREATE TABLE pro_user ( pro_id NUMBER PRIMARY KEY, active_location
	 * VARCHAR2(40), detail_category_id NUMBER, email varchar2(100), pro_name
	 * varchar2(100), password varchar2(100) );
	 */
}
